{
  "name": "tour-of-heroes-shared",
  "version": "1.0.0",
  "description": "Contains shared code for the different frameworks",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^2.6.2",
    "rxjs-tslint": "^0.1.5",
    "tslint": "^5.11.0"
  },
  "dependencies": {
    "rxjs": "6.2.1",
    "typescript": "2.8.3"
  }
}
